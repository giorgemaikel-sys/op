# ── Susan v3 — Requirements ───────────────────────────────────────────────────
# Instalar con: pip install -r requirements.txt

# ── GUI ───────────────────────────────────────────────────────────────────────
customtkinter>=5.2.2
Pillow>=10.0.0
pystray>=0.19.5

# ── Audio / TTS / STT ─────────────────────────────────────────────────────────
edge-tts>=6.1.9                 # TTS online (voz natural, requiere internet)
pyttsx3>=2.90                   # TTS offline de respaldo (voz de Windows, sin internet)
pygame>=2.5.2
sounddevice>=0.4.6
numpy>=1.24.0
SpeechRecognition>=3.10.0
vosk>=0.3.45

# ── Vision ────────────────────────────────────────────────────────────────────
opencv-python>=4.8.0
requests>=2.31.0

# ── Web / Scraping ────────────────────────────────────────────────────────────
beautifulsoup4>=4.12.0
duckduckgo-search>=3.9.0
httpx>=0.25.0
lxml>=4.9.0

# ── Email ─────────────────────────────────────────────────────────────────────
# imaplib y smtplib son stdlib — no requieren instalación

# ── Automatización de PC ──────────────────────────────────────────────────────
pyautogui>=0.9.54
pygetwindow>=0.0.9
pyperclip>=1.8.2
psutil>=5.9.0
pywin32>=306                    # Solo Windows

# ── OCR / Pantalla ────────────────────────────────────────────────────────────
pytesseract>=0.3.10             # Requiere Tesseract-OCR instalado
mss>=9.0.1                      # Captura de pantalla rápida

# ── Traducción ────────────────────────────────────────────────────────────────
deep-translator>=1.11.4

# ── Clima ────────────────────────────────────────────────────────────────────
# Se usa requests + DuckDuckGo (no requiere API key)

# ── WhatsApp ─────────────────────────────────────────────────────────────────
pyperclip>=1.8.2

# ── YouTube ──────────────────────────────────────────────────────────────────
yt-dlp>=2024.1.1

# ── Base de datos ─────────────────────────────────────────────────────────────
# sqlite3 es stdlib

# ── Utilidades ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
colorama>=0.4.6
